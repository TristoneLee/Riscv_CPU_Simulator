#include "cpu.hpp"

int main(){
//         freopen("sample.data", "r", stdin);
//         freopen("sample.out","w",stdout);
//         freopen("debug.out","w",stderr);
        cpu CPU;
        CPU.run();
        return 0;
}
